lowcap-bot is currently a CLI-first, mint-driven accumulation MVP backed by SQLite via Prisma.
The repo is organized around three fixed lanes: full import, mint-driven accumulation, and read-only inspection.
Full import still owns scoring, hard reject, optional metric create, and Telegram notify.
Mint-driven accumulation includes mint-only ingest plus source-specific semi-automation:
- import:mint, import:mint:file, import:mint:source-file
- detect:dexscreener:token-profiles
- detect:geckoterminal:new-pools
- token:enrich, token:rescore
- token:enrich-rescore:geckoterminal
- metric:add
- metric:snapshot:geckoterminal
Read-only inspection has lightweight views (`tokens:report`, `token:show`, `metrics:report`, `metric:show`), compare views (`tokens:compare-report`, `token:compare`), and Gecko-specific observation summary / timing helpers (`ops:summary:geckoterminal`, `compare:geckoterminal:dexscreener`).
This repo is still not a generic bot platform and does not include queue / worker / scheduler orchestration, a generic multi-source adapter runtime, or trading automation.

Latest checked state after CodexCLI update recovery:
- Date: 2026-05-23
- CodexCLI: codex-cli 0.133.0
- HEAD before consolidation: 7090996 docs: review auto send post execution state
- Token / Metric / Notification / HolderSnapshot: 1536 / 449 / 10 / 1
- Notification statuses: captured=5, sent=5, failed=0
- manual live-send candidate count: 0
- retry candidate count: 0
- enabled auto-send allowed candidate count: 0

Telegram / Notification completed slice:
- manual approved live send succeeded for id 7 and id 8
- SMOKE / REHEARSAL rows are excluded from live send and retry planning
- marker-tagged capture-only rehearsal succeeded as id 9
- production-shaped capture-only candidate id 10 was created
- notification:auto-send:plan is read-only / dry-run with allowlist, blockedReasons, and stopConditionCodes
- notification:auto-send:execute is disabled by default and requires --execute, NOTIFICATION_AUTO_SEND_ENABLED=true, and a passing planner gate
- production auto-send one-shot succeeded for id 10 and updated only that Notification to sent/live_send
- post-send stability is confirmed: id 10 is excluded from resend, failed=0, retry candidate=0, enabled auto-send allowed candidate=0

Still locked:
- scheduler
- systemd
- always-on auto live send
- restart duplicate-send behavior verification
- continuous worker / background queue
- automated notification retry execution
- production --execute without human approval

Do not run without explicit approval:
- production --execute
- notification send or retry execution
- Telegram live send
- metric snapshot write
- detect watch write
- ops catchup write
- --write / --watch / --live
- import / enrich / rescore writes
- scheduler / systemd
- schema / migration
- app code change for docs-only tasks

Recommended next task:
Green: review bounded new-pool watch readiness before Red rehearsal.
Risk: Green while read-only / docs-first; Red only if a later task runs --write, --watch, or checkpoint-changing execution.

Second choice:
Metric accumulation / report lane, if the next chat should prioritize safer data quality over monitoring-loop readiness.

Do not choose next:
Scheduler / systemd or always-on auto live send. Auto-send is single-shot verified only.

Read authority docs in this order:
1. AGENTS.md
2. README.md
3. docs/current-status.md
4. docs/architecture.md
5. docs/implementation-roadmap.md
6. docs/roadmap.md
7. docs/runbooks/auto-live-send-gate.md
