lowcap-bot is currently a CLI-first, mint-driven accumulation MVP backed by SQLite via Prisma.
The repo is organized around three fixed lanes: full import, mint-driven accumulation, and read-only inspection.
Full import still owns scoring, hard reject, optional metric create, and Telegram notify.
Mint-driven accumulation includes mint-only ingest plus source-specific semi-automation:
- import:mint, import:mint:file, import:mint:source-file
- detect:dexscreener:token-profiles
- detect:geckoterminal:new-pools
- token:enrich, token:rescore
- token:enrich-rescore:geckoterminal
- metric:add
- metric:snapshot:geckoterminal
Read-only inspection has lightweight views (`tokens:report`, `token:show`, `metrics:report`, `metric:show`), compare views (`tokens:compare-report`, `token:compare`), and Gecko-specific observation summary / timing helpers (`ops:summary:geckoterminal`, `compare:geckoterminal:dexscreener`).
This repo is still not a generic bot platform and does not include queue / worker / scheduler orchestration, a generic multi-source adapter runtime, or trading automation.

Latest checked state after offensive-safe enrich backlog consolidation:
- Date: 2026-05-24
- CodexCLI: codex-cli 0.133.0
- HEAD before consolidation: a2ab80a docs: review eighth enriched backlog batch
- Token / Metric / Notification / HolderSnapshot: 1541 / 459 / 10 / 1
- Metric distribution: 0=1222, 1=232, 2+=87
- Notification statuses: captured=5, sent=5, failed=0
- failed count: 0
- retry candidate count: 0
- enabled auto-send allowed candidate count: 0
- review:queue --pumpOnly --sinceHours 168:
  geckoOriginTokenCount=245, enrichPendingCount=200,
  metricPendingCount=85, staleReviewCount=200, notifyCandidateCount=0

Recent enrich backlog progress:
- 8 bounded 168h enrich/rescore Red batches completed successfully
- processed ids: 5619..5580, 40 tokens total
- enrichPending moved 240 -> 200
- each batch had selected=5, enriched=5, rescored=5, contextWritten=5,
  metaplexAttempted=5, metaplexAvailable=0, notifyWouldSend=0, notifySent=0
- provider error=0, 429=0, retry=0
- side effects stayed Token update only; Metric write, Notification
  create/update, HolderSnapshot write, Telegram send, scheduler/systemd, and
  rawJson full dump stayed 0

Processed cohort quality:
- metadataStatus partial=40
- scoreRank distribution: C=39, B=1
- scoreTotal distribution: 0=36, 1=3, 2=1
- hardRejected=0, notifyCandidate=0
- website/X/Telegram/Metaplex/description/link presence=0
- notable safe examples: 5607 B/2; 5596, 5590, 5581 C/1
- ids 5584 and 5583 have offensive name/symbol values; never print their raw
  text in docs, handoff, or final reports. Use "[offensive term]" or
  count-based summaries only.

Telegram / Notification completed slice:
- manual approved live send succeeded for id 7 and id 8
- SMOKE / REHEARSAL rows are excluded from live send and retry planning
- marker-tagged capture-only rehearsal succeeded as id 9
- production-shaped capture-only candidate id 10 was created
- notification:auto-send:plan is read-only / dry-run with allowlist, blockedReasons, and stopConditionCodes
- notification:auto-send:execute is disabled by default and requires --execute, NOTIFICATION_AUTO_SEND_ENABLED=true, and a passing planner gate
- production auto-send one-shot succeeded for id 10 and updated only that Notification to sent/live_send
- post-send stability is confirmed: id 10 is excluded from resend, failed=0, retry candidate=0, enabled auto-send allowed candidate=0

Still locked:
- scheduler
- systemd
- always-on auto live send
- restart duplicate-send behavior verification
- continuous worker / background queue
- automated notification retry execution
- production --execute without human approval

Do not run without explicit approval:
- production --execute
- notification send or retry execution
- Telegram live send
- metric snapshot write
- detect watch write
- ops catchup write
- --write / --watch / --live
- import / enrich / rescore writes
- scheduler / systemd
- schema / migration
- app code change for docs-only tasks

Recommended next task:
Green: exact-mint Metric 0 backlog preflight.
Risk: Green. Stay read-only/docs-only; do not run metric:snapshot --write.
The latest metric backlog preflight found that current batch
metric:snapshot:geckoterminal selection is newest-first and does not reach the
Metric 0 backlog ids 5380..5464 at limits 5 / 20 / 30 / 75. A later exact-mint
Red, if approved, should use one known Metric 0 mint and include
--noNotificationCapture so exact --mint mode does not create a Notification
row.

Second choice:
Design or preflight a pending-first batch selector for true Metric backlog
accumulation. Current batch commands such as:
pnpm -s metric:snapshot:geckoterminal -- --pumpOnly --limit 20 --sinceMinutes 10080 --minGapMinutes 60 --interItemDelayMs 15000 --write
would write Metrics to already measured rows first and would not reduce
metricPendingCount=85.

Third choice:
Repeat limit 5 enrich backlog Red after a fresh Green preflight and human
approval if the operator wants to return to Token update backlog. Known command
if selected later:
pnpm -s token:enrich-rescore:geckoterminal -- --pumpOnly --limit 5 --sinceMinutes 10080 --write

Do not choose next:
Scheduler / systemd or always-on auto live send. Auto-send is single-shot verified only.

Read authority docs in this order:
1. AGENTS.md
2. README.md
3. docs/current-status.md
4. docs/architecture.md
5. docs/implementation-roadmap.md
6. docs/roadmap.md
7. docs/runbooks/auto-live-send-gate.md
